# Env is configured per app, not from this root file:
#   apps/api/.env.example        -> copy to apps/api/.env
#   apps/storefront/.env.example -> copy to apps/storefront/.env
# See README.md "Quick start".
